import type { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Profile",
  description: "View and edit your Handcrafted Haven profile.",
};

function page() {
  return <div>profile</div>;
}

export default page;
